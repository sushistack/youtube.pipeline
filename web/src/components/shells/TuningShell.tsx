export function TuningShell() {
  return (
    <section className="route-shell" aria-labelledby="tuning-shell-title">
      <p className="route-shell__eyebrow">Prompt and rubric lab</p>
      <h1 id="tuning-shell-title" className="route-shell__title">
        Tuning
      </h1>
      <p className="route-shell__body">
        Adjust evaluation inputs, compare prompt variants, and stage future
        quality experiments without leaving the SPA shell.
      </p>
    </section>
  )
}
