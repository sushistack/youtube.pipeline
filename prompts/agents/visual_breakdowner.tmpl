# Stage 3.5 (v2): Visual Breakdown — Per-Act

You convert one act's continuous Korean monologue into a schema-ready shot breakdown for downstream image rendering.

The act monologue has already been segmented into ordered `BeatAnchor` slices by the upstream beat segmenter (writer stage 2). Produce **one shot per BeatAnchor, in beat order**. The shot's `narration_anchor` MUST mirror the input BeatAnchor byte-for-byte — you are NOT re-segmenting the monologue, you are attaching one visual descriptor to each pre-anchored beat.

Return JSON only. Do not use markdown fences.

## Act Context

- Act ID: `{act_id}`
- Act Mood (overall): `{act_mood}`
- Required Shot Count: `{shot_count}`  ← equals `len(beats)` below; output exactly this many shots

## Act Monologue (immutable — for context only; do NOT echo or rewrite)

```
{monologue}
```

## Ordered Beat Anchors (one shot per beat, in this order)

Each beat below carries the rune-offset slice of the monologue you are illustrating, plus the canonical metadata that anchors the visual identity. The beat's metadata fields (mood, location, characters_present, entity_visible, color_palette, atmosphere, fact_tags) are LOAD-BEARING — your `narration_anchor` block MUST echo every field exactly as supplied.

{beats_table}

## Frozen Visual Identity

Use this descriptor as the source of truth for visual identity (proportions, palette, distinguishing features). You no longer need to copy it verbatim into every `visual_descriptor` — instead, **preserve visual-identity continuity across shots while shifting focal subject per beat**:

{frozen_descriptor}

## SCP Visual Identity (full reference)

{scp_visual_reference}

## Output Contract

Return:

```
{
  "act_id": "{act_id}",
  "shots": [
    {
      "visual_descriptor": "act-specific visual planning text that preserves identity continuity for this beat",
      "transition": "ken_burns",
      "narration_anchor": {
        "start_offset": 0,
        "end_offset": 95,
        "mood": "...",
        "location": "...",
        "characters_present": ["..."],
        "entity_visible": true,
        "color_palette": "...",
        "atmosphere": "...",
        "fact_tags": [{"key": "...", "content": "..."}]
      }
    }
  ]
}
```

Rules:
- `act_id` MUST equal `{act_id}`
- output exactly `{shot_count}` items in `shots` (one per beat above)
- shot ordering MUST match beat ordering: `shots[i].narration_anchor` MUST be the i-th beat from the input table, with EVERY field copied byte-for-byte (start_offset, end_offset, mood, location, characters_present, entity_visible, color_palette, atmosphere, fact_tags). The downstream validator rejects any drift.
- every `visual_descriptor` must be non-empty
- every `visual_descriptor` must preserve visual identity (entity proportions, palette, distinguishing features) so all shots feel cohesive across the video — but **shift the focal subject per beat** (entity / environment / character POV / artifact close-up). Do NOT make every shot start with the same identity description.
- the `transition` field MUST be present and non-empty on every shot — omitting it or returning `""` is a contract violation
- the `transition` value MUST be exactly one of: `ken_burns`, `cross_dissolve`, `hard_cut` — no other values are permitted
- focus on factual consistency with each beat's monologue slice (use `start_offset`/`end_offset` to locate the precise text) and the SCP visual identity
- do not add commentary, explanations, or extra keys
