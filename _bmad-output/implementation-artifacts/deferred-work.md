# Deferred Work

## Deferred from: code review of 1-1-go-react-spa-project-scaffolding-build-chain (2026-04-16)

- `-race` flag requires CGO but Makefile uses `CGO_ENABLED=0` — architecture doc internally inconsistent. Resolve when adding CI pipeline (Story 1.7): either split `test-race` target or enable CGO for test target only.
- AC-GITIGNORE spec text says `web/dist/` but actual build output goes to `internal/web/dist/` — update story AC text to match chosen Vite outDir architecture.

## Deferred from: code review of 1-2-sqlite-database-migration-infrastructure (2026-04-16)

- WAL sidecar files (-wal, -shm) inherit default umask permissions instead of 0600. Spec says "DB file" (singular); fixing requires process-level umask or post-hoc chmod of lazily-created files. Localhost-only tool mitigates risk.
- `runs.updated_at` column has DEFAULT but no AFTER UPDATE trigger. Column will be stale after any UPDATE. Add trigger in a future migration when UPDATE operations are implemented (Epic 2).

## Deferred from: code review of 1-3-domain-types-error-system-architecture-interfaces (2026-04-17)

- DomainError sentinel pointers are mutable `var` — any code could mutate fields. Internal package + single-user tool mitigates risk. Consider unexported fields with getters if domain/ becomes shared.
- `Stage("")` is a valid value with no validation — add `IsValid()` when state machine is implemented (Epic 2).
- TextRequest/ImageRequest zero-values have no validation — add `Validate()` methods when provider implementations are built (Epic 5).

## Deferred from: code review of 1-4-test-infrastructure-external-api-isolation (2026-04-17)

- `BlockExternalHTTP` mutates global `http.DefaultTransport` without synchronization — not goroutine-safe. Layer 1 (constructor injection) covers parallel test scenarios. Add mutex if `t.Parallel()` + `BlockExternalHTTP` combination becomes needed.
- `Migrate` sets `PRAGMA user_version` outside transaction — crash between COMMIT and PRAGMA leaves inconsistent state. Pre-existing issue from Story 1.2, acceptable for single-user localhost tool.

## Deferred from: code review of 1-5-cli-foundation-init-doctor-commands (2026-04-17)

- `BindFlags` exported but dead code — `Load()` creates an internal viper instance so external pflags can't be bound. Redesign when CLI flag overrides for individual config values are needed (Story 1.6+ scope).
- `DefaultConfig()` and `DefaultConfigDir()` both compute `~/.youtube-pipeline` independently, both silently ignore `os.UserHomeDir()` error. Single-user desktop tool mitigates; revisit if containerized deployment is ever considered.
- `WriterCriticCheck` returns "must use different providers" when both providers are empty string — technically correct but misleading. Improve when Epic 2 adds full provider validation.
- `godotenv.Load()` permanently mutates process-level environment variables. Acceptable for single-process CLI but may cause env leak in tests calling `Load()` multiple times.

## Deferred from: code review of 1-6-cli-renderer-output-formatting (2026-04-17)

- ANSI color codes emitted unconditionally without TTY detection, NO_COLOR env check, or Windows ANSI VT support verification. Localhost single-operator tool mitigates. Add `golang.org/x/term.IsTerminal` or `NO_COLOR` check when piped/redirected output becomes a use case.
- TOCTOU race in `writeConfigIfNotExists` and `writeEnvIfNotExists` (pre-existing from Story 1.5). Use `os.OpenFile` with `O_CREATE|O_EXCL` for atomic create-if-not-exists.
- `database.Close()` return value silently discarded in init.go (pre-existing from Story 1.5). Add `defer` with error check when init becomes more complex.

## Deferred from: code review of 1-7-ci-pipeline-e2e-smoke-test (2026-04-17)

- `BlockExternalHTTP` mutates global `http.DefaultTransport` without synchronization — not goroutine-safe for parallel tests. Pre-existing from Story 1.4; add mutex when `t.Parallel()` is introduced.

## Deferred from: code review of 2-1-state-machine-core-stage-transitions (2026-04-17)

- `NextStage` does not distinguish "unknown stage" from "invalid transition for known stage" — same error format for both. Input validation is caller responsibility; adding guards would exceed ~100 LoC target.
- Hardcoded magic numbers (15, 45) in `engine_test.go` guard assertions — fragile if stages/events are added. Intentional as documentation; tests WILL fail on enum changes (desired behavior).
- `StatusForStage` returns `StatusRunning` for unknown/invalid Stage values without error. Function is only called with validated stages from DB; signature change not warranted for V1.
- `TestNextStage_InvalidTransitions` checks `err != nil` but does not assert error message content includes stage and event strings. Minor test thoroughness gap.
- `BlockExternalHTTP` only intercepts `http.DefaultTransport` — custom-transport HTTP clients bypass the block. Architecture limitation of Layer 2 defense; Layer 1 (constructor injection) is the primary guard.
- `test-e2e` job uses `continue-on-error: true` which masks real failures. Remove once `pipeline serve` command is implemented (Epic 6) and add `needs: [test-e2e]` to `build` job.

## Deferred from: code review of 2-2-run-create-cancel-inspect (2026-04-17)

- Vite reverse-proxy error responses in `pipeline serve --dev` bypass the api middleware chain — no `X-Request-ID`, no slog JSON line, 502 from stdlib log. Acceptable during dev-only usage; revisit when web UI stabilizes in Epic 6.
- `newRequestID` falls back to the string `"unknown"` when `crypto/rand.Read` returns an error. Concurrent requests all get the same ID, breaking log correlation. Theoretical — `/dev/urandom` has not been observed to fail on Linux. Add monotonic-counter fallback if it ever becomes an issue.

## Deferred from: code review of 2-3-stage-level-resume-artifact-lifecycle (2026-04-17)

- Permission-denied mid-`CleanStageArtifacts` leaves partial on-disk state. `os.RemoveAll` aborts on first error; re-running Resume after permission fix completes the cleanup (idempotent). V1 accept.
- Context cancellation not honored inside `os.RemoveAll` — stdlib constraint. Torn-state risk is addressed by the atomicity patch on `ResumeWithOptions`.
- `scanEpisode` fails completely when a `segments.shots` TEXT column has malformed JSON. A single bad row blocks all Resumes for the run until manual DB intervention. Graceful-skip semantics deferred to V1.5.
- DB lock under concurrent `pipeline serve` + `pipeline resume` on the same SQLite file. `busy_timeout=5000` already applied at `OpenDB`; single-operator tool mitigates risk.
- Reload error after successful engine mutation surfaces to caller as a resume-failure even though state was committed. Retrying then produces `ErrConflict`. Add idempotent acknowledgment semantics in a future revision.

## Deferred from: implementation of 2-4-per-stage-observability-cost-tracking (2026-04-18)

- **Cost accumulator priming on process restart.** When Story 3.1 wires the engine, `CostAccumulator` should be seeded with `runs.cost_usd` so the per-run cap survives a crash/restart. Not implemented here because there is no caller yet — dead code deferred.
- **Jitter seed via clock interface.** `WithRetry`'s jitter uses a package-level `math/rand` seeded from `time.Now().UnixNano()` — cannot be made fully deterministic in tests without a package-level seed swap. Tests assert elapsed-time *bounds* instead. Thread jitter through `clock.Clock` when it gains a `Rand()` surface.
- **Per-stage cost history for a single run.** V1 aggregates cost into one column per run; "cost by stage for one run" requires a separate observability history table. Story 2.7's metrics CLI may surface this gap.
- **Emergency cap override.** No `--ignore-cost-cap` CLI flag. Operators who want to push past `CostCapPerRun` for a critical run must edit `~/.youtube-pipeline/config.yaml`. Deferred to Epic 10.
- **Per-shot cost attribution in Phase B.** Image track records one `StageObservation` per shot → all sums collapse onto `StageImage`. Fine for cost totals, useless for per-shot forensics. V1.5 may add `shot_index` to observability when Story 5.4 ships.
- **`retry_reason` nil-preservation via COALESCE is opinionated.** Passing `nil` means "leave retry_reason alone". A `Recorder.ClearRetryReason(runID)` would be additive; add only if the engine ever needs to clear outside the Resume path (which already clears via `ResetForResume`).
- **Linter consolidation carried forward from Story 2.3.** `SetStatus` + `IncrementRetryCount` were merged into `ResetForResume`; `SetStage` was deleted as dead code; `Engine.Resume` now returns `(*InconsistencyReport, error)`; `ClearClipPathsByRunID` was added for assemble-stage resume. Callers in `service`, `cmd/pipeline/resume.go`, and `internal/api/handler_run.go` were threaded to carry the report through as `warnings`. `docs/contracts/run.resume.response.json` was not touched — verify the contract test still matches the threaded shape in the next code review.
- **Consistency check strictness regression (linter-introduced).** `CheckConsistency` now emits `run_directory_missing` for post-Phase-A runs when the runDir is absent. The pre-existing test `TestCheckConsistency_EmptyScenarioPathTreatedAsMissing` was replaced with `TestCheckConsistency_EmptyScenarioPathTreatedAsNil`. Confirm this flip matches Story 2.3's intent in its code review.
- **Engine.Advance remains a stub.** Story 2.4's `Recorder` + `CostAccumulator` are designed to drop into `Advance` when Epic 3 lands; no wiring today.

## Deferred from: code review of 2-4-per-stage-observability-cost-tracking (2026-04-18)

- **FakeClock backoff test harness uses wall-clock polling.** `internal/llmclient/retry_test.go` drives a `for { clk.Advance(1s); time.Sleep(1ms) }` loop with a 2s real-time deadline to wake the tested goroutine. Not fully deterministic but loose-bounded assertions pass reliably. Fix when `clock.Clock` gains a `Rand()`/`Yield()` surface so jitter and goroutine coordination flow through the fake.
- **`WithRetry` auto-retries on `ErrStageFailed`.** `RetryReasonFor(ErrStageFailed) = "stage_failed"` → retryable. Architecture [architecture.md:617] describes ErrStageFailed as "retryable (manual resume)" — auto-retry may mask systematic 500s. Left as-is for V1 (maxRetries caps the damage); re-evaluate once Epic 5 wires the real Phase B and operators observe retry behavior.
- **`docs/cli-diagnostics.md` ↔ `internal/db/diagnostic_query_test.go` drift risk.** Both embed the same SQL verbatim. Add explicit cross-references in both files OR derive one from the other (V1.5).
- **Contract fixture re-validation after linter's Resume signature change.** `testdata/contracts/run.resume.response.json` not visibly updated when the linter flipped `Engine.Resume` to return `(*InconsistencyReport, error)` during Story 2.4. Re-run the contract test explicitly in the next story touching `internal/api`.

## Deferred from: implementation of 2-5-anti-progress-detection (2026-04-17)

- **Embedding-based cosine (V1.5).** V1 `pipeline.CosineSimilarity` is bag-of-words term-frequency cosine — deterministic, LLM-free, and satisfies FR8 + NFR-R2 V1 requirements. V1.5 should swap the implementation (same signature) to embedding-based vectors via DashScope. Detector API stays unchanged.
- **Ground-truth FP signal for NFR-R2.** V1 uses `runs.human_override=1` as an FP-rate proxy in `AntiProgressFalsePositiveStats`. V1.5 needs a concrete definition, e.g., "operator rejected the anti-progress stop AND a subsequent retry produced a Critic-pass output within the same run." Requires operator-decision plumbing from Epic 7/8.
- **Canonical `retry_reason` enum.** The four literal strings (`"rate_limit"`, `"timeout"`, `"stage_failed"`, `"anti_progress"`) are still scattered across `internal/llmclient/retry.go`, `internal/pipeline/observability.go`, test fixtures, and docs. Consolidate into `domain.RetryReason` + const block in a small follow-up story; low risk but high clarity win.
- **Detector + cost-cap interaction ordering.** If a retry loop simultaneously trips cost-cap (`ErrCostCapExceeded`) AND anti-progress (`ErrAntiProgress`), cost-cap wins in V1 because `WithRetry` + caller will observe the cap error first and short-circuit before the next detector check. Document if this ordering produces confusing `retry_reason` values in practice.
- **Anti-progress operator escalation UX.** Story 2.5 surfaces the error code + message, but does NOT wire a HITL pause stage or a UI surface for the "Retries producing similar output — human review required" message. Epic 3's Critic loop (3.3) + Epic 7/8 web surfaces own this.
- **Detector goroutine-safety.** Single-caller-per-retry-loop contract is documented but not enforced. If Epic 3 ever needs parallel detectors for different sub-tasks within a stage, each goroutine must instantiate its own; a shared instance is a data race. Add a mutex if this constraint becomes inconvenient.
- **`AntiProgressThreshold` + `CostCapPerRun` viper defaults were both missing in `loader.go`.** Story 2.5 adds `v.SetDefault("cost_cap_per_run", ...)` + `v.SetDefault("anti_progress_threshold", ...)` to make the default contract explicit. In practice `v.Unmarshal(&cfg)` against a `cfg` pre-seeded from `DefaultConfig()` left `CostCapPerRun` at 5.00 because viper's mapstructure path only overwrites fields for keys it knows — so the pre-existing missing `SetDefault` was not a zero-overwrite bug, just an inconsistency with the rest of the `cost_cap_*` block.
- **`go test -race` under `CGO_ENABLED=0`.** The Story 2.5 full-sweep runs without `-race` because the project-wide CGO=0 constraint disables the race detector. Same tension flagged in Story 1.1 deferred-work; resolution deferred until a `test-race` make target (CGO on) lands.
- **Pipeline similarity → separate package.** Current home (`internal/pipeline/similarity.go`) is correct for V1. When V1.5's embedding-based implementation lands and starts calling DashScope, consider moving the vectorization into `internal/llmclient/embed.go` and keeping only the detector in `pipeline/`.

## Deferred from: code review of 2-5-anti-progress-detection (2026-04-18)

- **Anti-progress warn-before-persist drift.** `Recorder.RecordAntiProgress` emits the `slog.Warn("anti-progress detected")` line BEFORE calling `Record(...)`, so a DB error would leave an alerting signal without a corresponding `runs.retry_reason='anti_progress'` row. Documented behavior, but in Epic 3/7 escalation UX this becomes an obs-vs-DB reconciliation gap. Consider adding a "persist failed" follow-up warn or swapping the order once stream-handlers land.
- **V1 bag-of-words Tokenize is Korean-weak.** Korean (primary project content language) has no word boundaries by whitespace for compound predicates, Hangul is a no-op under `strings.ToLower`, and 조사/어미 inflections make semantically-identical sentences tokenize to near-disjoint sets. V1 detector will over-trigger FPs on Korean Writer outputs. V1.5 embeddings (already deferred) fix this natively. Explicitly calling out Korean here so the V1.5 migration prioritizes this user segment.
- **`AntiProgressDetector` goroutine-safety is doc-only.** No mutex, no race test, no runtime assertion. Single-caller-per-retry-loop is honored by V1 design (Epic 3's Critic loop is sequential per stage). Add enforcement only if Epic 3 changes the contract — otherwise dead weight.
- **AC-12 additional inline scenarios not implemented.** Spec's AC-RUNSTORE-ANTI-PROGRESS-WINDOW listed two illustrative inline cases (`{30 anti_progress, 20 override} window=50 → Provisional:true`; `{60, 3} window=50 → Provisional:false`). Fixture-based `TestAntiProgressFalsePositiveStats_RollingWindow` pins both the `Provisional=true` and `OperatorOverride>0` invariants, so the extra inline cases are redundant for V1.
- **Composite `(retry_reason, created_at DESC)` index.** `AntiProgressFalsePositiveStats` subquery today leans on `idx_runs_created_at` for ORDER BY and filters `retry_reason='anti_progress'` in-memory. When anti-progress rows are sparse relative to total `runs` volume, SQLite cannot early-terminate on `LIMIT` without a composite index. Acceptable in V1 (few rows, query runs rarely) but flag if Epic 10's metrics CLI starts hammering this. Requires a Migration 004.

## Deferred from: implementation of 2-6-hitl-session-pause-resume-change-diff (2026-04-18)

- **Decision-capture API endpoint (Epic 8).** POST /api/runs/{id}/decisions is the canonical write path that should call `pipeline.UpsertSessionFromState` after every HITL action. Story 2.6 ships the pause-state storage + read path (FR49/FR50) but not the write endpoint. Story 8.2 picks this up; until then `hitl_sessions` rows are only populated via fixtures or test seeds.
- **HITL session TTL cleanup.** A run that stays `waiting` for weeks accumulates a stale snapshot. The `last_interaction_timestamp` is the natural aging key. Add a `pipeline hitl sweep` CLI or a background sweeper when run inventory grows; V1 has no background jobs so this is intentional.
- **Segment-level timestamps for diff.** FR50 diff today matches on `(decision_type, scene_id)` and pulls the timestamp from `decisions.created_at`. Run-level deltas (retry_count, cost_usd, critic_score changes between T1 and T2) are NOT surfaced in the diff. If operators request "cost changed from $1.25 to $2.10 while you were away" during post-V1 review, add a run-level delta section in V1.5.
- **Scene-id numeric sort robustness.** `pipeline.SnapshotDiff` uses numeric-aware ordering when scene_ids parse as ints; lexicographic fallback otherwise. V1 always emits numeric string scene_ids, so this is a defensive choice. Revisit if Epic 3 ever introduces non-numeric scene identifiers (UUIDs, hashes).
- **`snapshot_json` schema versioning.** `domain.DecisionSnapshot` has no explicit `"v"` field. Adding fields later will parse cleanly on old rows (Go json-unmarshal ignores unknown keys); removing fields will silently drop them. If the struct changes shape in V1.5, add a `"version": 1` field and migration logic at that point.
- **HITL session invariant audit CLI.** A one-off `pipeline hitl audit` that scans for (a) orphan sessions (run not in HITL state but session row present) and (b) ghost sessions (HITL-waiting run with no session row) would catch invariant violations that accumulate over time. Fold findings into `doctor`. Deferred as a Story 10.x diagnostics cleanup.
- **`service → pipeline` import edge.** `scripts/lintlayers/main.go:27` already allows `internal/service → internal/pipeline`, so the HITLService's use of `pipeline.BuildSessionSnapshot`, `pipeline.SnapshotDiff`, and `pipeline.Change` required no lint-rule change. If the edge ever grows uncomfortable, refactor the diff computation behind a narrower interface owned by service/ so the import reverses direction.
- **Migration ordinal collision with 2-5 in-flight work.** Story 2.5's code review phase added `migrations/004_anti_progress_index.sql` (composite retry_reason index) at the same ordinal as this story's `004_hitl_sessions.sql`. Both apply cleanly because `db.Migrate` captures `current` once per run, so each file with `ver > initial_current` is applied in lexicographic order without reading back the incremented user_version. Renumbering should happen when the next sprint planning pass normalises migration IDs — don't touch it mid-story to avoid breaking staged DBs.
- **`TestRollingWindowQueries_UseIndexes/stage_histogram_in_window` pre-existing failure.** Story 2.7's externally-added check `strings.Contains(planStr, "SCAN runs")` trips on the `stage_histogram_in_window` query even though SQLite's `SCAN runs USING INDEX idx_runs_stage` is an efficient index scan (not a table scan). This is Story 2.7's false-positive assertion, unrelated to 2.6 code. Fix belongs to whoever closes 2.7: either relax the assertion to `strings.Contains(planStr, "SCAN runs\n")` or distinguish "SCAN ... USING INDEX" from bare "SCAN ...".

## Deferred from: implementation of 2-7-pipeline-metrics-cli-report (2026-04-18)

- **V1 automation-rate granularity is run-level, not stage-level.** `runs.human_override` is a sticky bit across all stages of a run, so the metric reports "fraction of runs with zero operator intervention" rather than the PRD's literal stage-level ratio. Good enough for Day-90 gating; per-stage observability rows should replace it in V1.5.
- **V1 kappa is run-level, not scene-level.** We infer one dominant operator verdict per run because V1 has no `decisions.critic_verdict` column. Epic 8's richer decision-capture path should promote this to scene-level calibration.
- **`--regression-rate` and `--idempotency-rate` are file-based pass-throughs.** Story 2.7 surfaces both metrics without fabricating values, but the harnesses that generate those files remain out of scope. Epic 10 can graduate this into a first-class internal workflow.
- **Calibration threshold is CLI-flag based, not config-file based.** `--calibration-threshold` defaults to 0.70 for V1 sensitivity analysis. Promote it to `pipeline_config.yaml` only after Epic 4 locks the long-term threshold contract.
- **Kappa float precision is strict.** `TestCohensKappa_KnownTextbookExample` and the fixture-backed report test assert tight IEEE-754 results. Re-verify if the project ever adds a broader architecture/CPU CI matrix.
- **Migration ordinals remain fragile.** Story 2.7 correctly uses `005_metrics_indexes.sql`, but the repo still has duplicate `004_*.sql` files. Add a migration-lint rule when schema churn grows further.

## Deferred from: code review of 2-7-pipeline-metrics-cli-report (2026-04-18)

- **Float-precision edge in file-sourced rate vs target comparison.** Theoretical: a user-supplied `--regression-rate` value of `0.800001` would fail a `≥ 0.80` target despite operator intent. Both sides parse identically in practice; add epsilon tolerance only if operators complain.
- **`ORDER BY created_at DESC, id DESC` lexical sort on run IDs embedding numbers.** `internal/db/run_store.go` — `scp-049-run-10` sorts before `scp-049-run-2`. Spec-pinned pattern (matches Migration 004 precedent). Deterministic but surprising when fixture ties on created_at. Revisit alongside any run-ID schema rework.
- **Provisional line + all-unavailable rows is noisy UX.** When `WindowCount=0`, the human output shows both `[provisional — n < 25]` and five "unavailable" rows. Collapse into a single empty-DB banner once the CLI has a real first user.
- **Negative kappa renders without a "worse than chance" banner.** `cmd/pipeline/render.go` — fine for V1 (fail symbol + red), but a κ < 0 indicates active miscalibration. Epic 10 UX polish.
- **`metricsClock` is a package-level mutable seam for tests.** `cmd/pipeline/metrics.go` — tests save/restore the var; parallel CLI invocations do not exist in V1. Revisit when an API handler surfaces the same path.
- **`valueMetric` default comparator branch silently falls through to GTE.** `internal/service/metrics_service.go` — acceptable because `MetricComparator` is a closed const set in V1. Add a panic/log branch when introducing a third comparator.
- **`AntiProgressFalsePositiveStats` Provisional semantics.** `internal/db/run_store.go` — `Provisional: total < window` flags a healthy system as provisional. Pre-existing from Story 2.5; fix alongside Story 2.5 retro.
- **Golden files only exercise the all-available row shape.** `testdata/golden/cli_metrics_json.json` — the `unavailable:true + value:null + reason:"…"` shape is covered by service unit tests. Add a second golden if unavailable-row regressions appear.
- **`KappaPairsForRuns` assumes per-run `critic_score` invariance under GROUP BY.** `internal/db/decision_store.go` — structurally held by `GROUP BY … r.critic_score`, but silently relies on runs-table immutability of that column. Document the invariant if the column ever becomes mutable.
- **`decisions.scene_id` should be INTEGER, not TEXT.** `DefectEscapeInRuns` joins on `CAST(s.scene_index AS TEXT) = d.scene_id` — fragile if scene_id ever uses non-numeric/padded strings. V1 writer always emits numeric strings so no current bug, but the correct fix is a `migration 006` table-recreation + `domain/types.go SceneID *string → *int` + hitl_diff/hitl_session updates. Do this before Epic 3 when the writer first writes scene_id values.

## Deferred from: code review of 2-6-hitl-session-pause-resume-change-diff (2026-04-18)

- **Story 2.7 scope creep in `decision_store.go`:** `KappaPairsForRuns` + `DefectEscapeInRuns` were added to the 2.6 file by parallel 2.7 work. `KappaPairsForRuns` has a decision-count vs scene-count aggregation gap (scenes with multiple non-superseded decisions skew the kappa verdict). Belongs to Story 2.7's review queue.
- **Multi-decision-type-per-scene inflation + pending clamp masking:** `DecisionCountsByRunID` counts approve and reject via independent `COUNT(DISTINCT scene_id)` subqueries. If a scene has both a non-superseded approve AND a non-superseded reject, the scene is counted in both `Approved` and `Rejected`, and the service's `if PendingCount < 0 { PendingCount = 0 }` clamp silently masks the anomaly (no log, no error). V1 Epic 8 decision endpoint will enforce single-non-superseded-decision-per-scene; until then, anomaly is invisible.
- **CLI metrics Unicode column padding:** `renderMetrics` in cmd/pipeline/render.go pads via `%-9s` on strings containing `≥`/`≤` (3 bytes each in UTF-8), so column alignment breaks visually. Story 2.7 scope.
- **Artificial seed state in `TestEngine_Resume_RemovesHITLSession_WhenExitingHITL`:** The test seeds `stage=write + status=waiting`, which is not reachable from the 2.1 state machine. When Epic 3 wires the HITL-exit transitions, replace with a reachable state (e.g., `batch_review + waiting → ResetForResume → approving event pushes to assemble/running`).
- **Migration 004 ordinal collision:** Already tracked; `004_hitl_sessions.sql` shares an ordinal with Story 2.5's `004_anti_progress_index.sql`. Renumber on the next sprint planning normalization pass (do NOT rename mid-story — would break staged dev DBs).
- **Test-name / helper-path substitutions:** Spec requested `TestMigrate_004_HITLSessionsTable` + `testutil.AssertJSONEqual`; implementation delivered `TestSchema_HITLSessionsColumns` + `TestSchema_HITLSessionsUpdatedAtTrigger` + inline `assertJSONStructuralMatch`. Functionally equivalent; style divergence from the spec's exact names.
- **BuildStatus non-transactional read race:** `GetSession` + `ListByRunID` in `HITLService.BuildStatus` execute as separate statements. A concurrent `UpsertSessionFromState` can interleave between them, producing a snapshot/live pair that's internally inconsistent — the diff may show "no changes" when scenes in fact changed. V1 is single-operator so this race window isn't reachable in normal use. Wrap in a read transaction when Epic 7/8 add concurrent write paths.

## Deferred from: code review of 3-1-agent-function-chain-pipeline-runner (2026-04-18)

- **Directory fsync after Rename is skipped.** `writeScenario` performs `Sync` on the temp file before `Rename` but does not open `runDir` and call `fsync` on the directory inode. A power-loss between the rename syscall and the directory-inode flush could leave `scenario.json` unresolvable on reboot. Accept the risk for a single-operator desktop tool; add directory fsync when packaging for production-grade hosting.
- **Rename failure leaves prior scenario.json intact.** If `os.Rename(tmp, final)` fails with EBUSY/EACCES on an overwrite of an existing `scenario.json`, the old file survives unchanged and the caller sees an error. Operators must notice the error and decide whether the stale file is a problem. No automatic `.bak` fallback — acceptable because Rename failures on the same filesystem are rare.
- **outputDir traversal not validated.** `NewPhaseARunner` accepts any non-empty `outputDir` string — relative paths and embedded `..` components are allowed. Only `RunID` is validated against path-traversal (since that is the attacker-controlled component in the JSON input). Tighten if an untrusted config source ever populates `outputDir`.
- **StartedAt overwrite on resume.** Each `Run` call unconditionally overwrites `state.StartedAt`. When Story 3.5 wires resume semantics that re-invoke the chain, the original StartedAt is lost. Decide at 3.5 integration time whether to preserve on non-empty input or stamp a distinct `RestartedAt` field.
- **nestedTrackedPackages longest-match relies on insertion order ties.** `matchNestedTracked` returns the longest prefix, but ties are broken by iteration order of a slice. Only one entry exists today (`internal/pipeline/agents`) so this is moot. Add a length-sorted index when a second nested package lands.
- **Concurrent Run on the same runner is a documented contract, not enforced.** The runner struct is stateless between calls (all mutable state lives on the caller-provided `*PipelineState`), so concurrent calls with *distinct states* are in practice safe. Calls with the *same state* race on fields — the godoc says "NOT goroutine-safe"; add a `sync.Mutex` only if the runner ever becomes a public API surface.

- **ConsistencyChecker should reuse `pipeline.ScenarioPath`.** `internal/pipeline/consistency.go` currently duplicates the `filepath.Join(runDir, "scenario.json")` literal inline. Story 3.1 exports `ScenarioPath(outputDir, runID string)` as the single source of truth for the scenario.json file path, but does NOT refactor `consistency.go` to use it (scope creep). Follow-up after Story 3.5 integration wires the engine to the runner.
- **PipelineState typed output fields.** `internal/pipeline/agents/agent.go` uses `json.RawMessage` placeholders for the six agent outputs (Research, Structure, Narration, VisualBreakdown, Review, Critic). Stories 3.2–3.5 replace each with a typed domain struct (`ResearchSummary`, `ScenarioStructure`, `NarrationSet`, `ShotBreakdown`, `ReviewReport`, `CriticVerdict`). Do not pre-define them in 3.1.
- **Inverse mapping `domain.Stage → agents.PipelineStage`.** Story 3.5 will need this for Recorder wiring (stage observations indexed by domain.Stage). Not needed in 3.1; add alongside the engine integration.
- **`scenario.json` schema contract fixture.** `testdata/contracts/phase_a_state.json` will be introduced by Story 3.5 once the PipelineState fields are fully typed. 3.1 round-trips raw JSON without a frozen schema.
- **Per-agent Recorder integration.** PhaseARunner logs stage start/complete via slog but does NOT yet call `pipeline.Recorder` for cost/token accounting — those come online when agents are real (3.2–3.5). Wire Recorder into the runner in Story 3.5 when the first real agent has cost data to record.
- **Engine.Advance wiring.** Story 3.1 preserves the `advance not implemented: epic 3 scope` stub per AC-ENGINE-ADVANCE-UNCHANGED. Story 3.5 wires `PhaseARunner` into `Engine.Advance` after all six concrete agents exist.
- **LLM-driven Researcher & Structurer.** Story 3.2 shipped a deterministic V1 that reads /mnt/data/raw corpus and synthesizes `*domain.ResearcherOutput` / `*domain.StructurerOutput` by field copy + modulo distribution. The prompt templates at `docs/prompts/scenario/01_research.md` and `docs/prompts/scenario/02_structure.md` are unreferenced by Go code (guarded by `TestAgents_NoPromptFileReferences`). A V1.5 story will wire `domain.TextGenerator` through both agents, swap `SourceVersion` to `"v1.5-llm"`, keep the schema validators unchanged (they are the contract), and delete the no-prompt-references test. Until then V1 output has limited narrative quality — expect the first Golden-eval round (Epic 4) to measure the gap.

- **Production schema source for `agents.Validator`.** Story 3.2's `NewValidator(projectRoot, schemaFile)` resolves `testdata/contracts/*.schema.json` through a project-root anchor that only makes sense in tests. Production usage (when Story 3.5 wires `engine.Advance` → `PhaseARunner.Run`) will need a different source: either a `//go:embed schemas/*.json` inside `internal/pipeline/agents/` with a drift-check test against `testdata/contracts/`, or a config-specified on-disk path shipped alongside the binary. The choice is deliberately open here — premature embedding couples the validator to a layout Story 3.5 may want to change. Until Story 3.5 picks, Phase A in production is gated by the `engine.Advance` stub (Story 3.1 AC-ENGINE-ADVANCE-UNCHANGED), so no production path invokes the Validator with an undefined projectRoot.

## Deferred from: code review of 3-2-researcher-structurer-agents (2026-04-18)

- **Synopsis double-period in Structurer output.** The spec-prescribed format `"Act N opens with " + firstKeyPoint + ". (" + ...` produces strings like `"...platform light.. (2 beats..."` because key-point descriptions already terminate in `.`. The sample fixture bakes this in. Fix requires either (a) spec amendment to the format string or (b) trimming a trailing period from the key-point before concatenation. Defer to spec-owner decision; functional tests pass either way.
- **`distributeSceneBudget` has no target-range guard.** Schema pins `scene_budget ∈ [1,5]` and `target_scene_count ∈ [8,12]`, but the function accepts any `int`; targets ≥13 would yield `scene_budget=6` for Act 3 and fail schema validation. Caller is pinned at `target=10` today, so no runtime reach. Add a callable-contract guard when a later story parameterizes T.
- **`researcher_output.schema.json` duplicates Go-side `dramatic_beats` length bounds.** Schema declares `minItems:3 maxItems:10` matching the `buildDramaticBeats` trim + "sparse corpus" check. Belt-and-suspenders; harmless but a second source of truth that can drift from the Go derivation.
- **`structurer_output.schema.json` does not enforce act-level ordering, unique `id`, or `duration_ratio` sum.** Current Go test `TestStructurer_Run_ActDurationRatioSum` plus `ActOrder` constant guard these invariants — but an external consumer reading only the schema could produce a structurally "valid" but semantically wrong document (e.g., 4× `incident`). Strengthening via JSON Schema tuple validation (`prefixItems`) is additive.
- **No dedup on duplicate `key_visual_moments` / `anomalous_properties`.** Researcher appends each entry as a beat without dedup. Spec doesn't mandate dedup; no corpus currently has duplicates. Add a `seen map[string]bool` guard if a future golden fixture exposes this.
- **`truncateMainText` is O(N²) over input bytes.** The inner `utf8.RuneCountInString(s[:i])` call rescans the prefix on every iteration. Functionally correct within the 4000-rune cap; perf negligible for V1. Replace with a single-pass `utf8.DecodeRuneInString` accumulator when the main-text size envelope grows.
- **`describeValidation` has no depth/cycle guard.** Recursive walk over `jsonschema.ValidationError.Causes` could stack-overflow on a pathological schema. Schema inputs are owned by us (testdata/contracts/), so practical risk is zero.
- **Test quality cleanups.** (a) `TestResearcher_Run_CallsBlockExternalHTTP` name promises HTTP-blocking coverage but body is a happy-path run. (b) `TestResearcher_Run_SliceIsolation` only checks input→output aliasing direction, not reverse. (c) `TestResearcher_BeatTones_Rotate` relies on fixture content, not a function invariant. Rename / rewrite during the next test-polish pass.
- **`go.mod` elevates `gopkg.in/yaml.v3` from indirect to direct.** No new yaml import appears in the diff — likely a `go mod tidy` side effect or leftover from a deleted import. Clean up on the next adjacent story that touches `go.mod`.

## Deferred from: code review of 3-3-writer-agent-critic-post-writer-checkpoint (2026-04-18)

- **`writeScenario` missing parent-directory fsync.** After `os.Rename(tmp, scenario.json)`, the containing run directory is not fsync'd. On crash between rename and dir-fsync, some filesystems (ext4 without data=ordered, tmpfs→disk) can lose the rename. Since `scenario.json` is used as the "chain succeeded" tombstone, durability matters. Not introduced by Story 3.3; addresses pre-existing `PhaseARunner.writeScenario` architecture. Fix: add `dir, _ := os.Open(runDir); dir.Sync(); dir.Close()` after `os.Rename`.

## Deferred from: code review of 3-4-visualbreakdowner-reviewer-agents (2026-04-18)

- **Prompt placeholder silent fall-through.** `strings.NewReplacer` pattern (Writer, VisualBreakdowner, Reviewer) leaves unknown `{…}` tokens untouched — a stale or renamed placeholder silently ships the literal to the LLM. Fix well requires a load-time validator that parses `{\w+}` from every template and checks against each agent's known placeholder set. Broader pattern, not worth fixing piecemeal in 3.4.
- **`fr-coverage.json` FR12 annotation narrower than actual guard scope.** Reads "before the Writer stage runs" but `ValidateDistinctProviders` runs before every stage. Pre-existing Story 3.3 annotation; rewrite during next FR coverage pass.
- **`ScenarioPath(outputDir, runID)` accepts empty inputs.** Empty `runID` resolves two separate runs to the same path (`outputDir/scenario.json`). Runner currently rejects empties at construct time so production is safe; helper is only a footgun for external callers. Pre-existing Story 3.3.
- **`ShotCountForDuration` has no upper cap.** A 200s narration estimate still returns 5 shots, producing 40s Ken Burns holds that Phase B video assembly likely does not want. Schema does not constrain `estimated_tts_duration_s` upper bound either. Revisit when Phase B (Epic 5) defines its shot-duration envelope.
- **`PipelineState` round-trip non-identical for `ShotOverrides=nil`.** Agent always initializes to `{}`, but a human-edited `scenario.json` could introduce `"shot_overrides": null` and re-validation would reject as non-object. Currently only risky in Epic 7 manual-edit surface; add custom marshaler or `,omitempty` decision there.

## Deferred from: code review of 3-5-phase-a-completion-post-reviewer-critic (2026-04-18)

- **`scoreRubric` overwrites explicit 0 `OverallScore`.** `if OverallScore == 0 && Verdict != retry` applies rubric regardless of whether 0 was deliberate. LLM scoring "truly 0" is indistinguishable from "not set". Fix requires a sentinel (e.g., omitempty + distinct "not-set" default) or spec clarification.
- **`findProjectRoot` uses `os.Getwd()`.** Walks up from CWD to find `go.mod`; breaks if binary is invoked from a subdirectory or monorepo root. Works for normal CLI usage. Harden by accepting `schemaRoot` parameter when a non-CWD deployment model is needed.
- **`os.Rename` atomicity on Windows / cross-filesystem (EXDEV).** `os.Rename` over existing destination file fails on Windows; across filesystems returns EXDEV. Linux-primary project; revisit when Windows packaging is needed.
- **`CriticRubricWeights` is an exported mutable map.** Any test or consumer can mutate it globally. Use unexported or `sync.Once`-cloned copy when the weights table becomes a configuration surface.
